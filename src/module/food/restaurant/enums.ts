export enum OrderByColumn {
  CREATED_AT = 'created_at',
  UPDATED_AT = 'updated_at',
}

export enum RestaurantSortBy {
  ORDER_COUNT = 'orders_count',
  DELIVERY_TIME = 'delivery_time',
  LIKE_COUNT = 'like_count',
  DISTANCE = 'distance',
}

export enum ValidImageFileExtension {
  PNG = 'png',
  JPEG = 'jpeg',
  JPG = 'jpg',
}
export enum ValidDOCFileExtension {
  PDF = 'pdf',
  DOC = 'doc',
  DOCX = 'docx',
  ODT = 'odt',
}

export enum ValidFileType {
  IMAGE = 'image',
  DOCUMENT = 'document',
}
